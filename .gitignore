/target
/npm/out
node_modules
.DS_Store

# Local environment files (may contain API keys). .env.example stays tracked;
# none of these patterns match it.
.env
.env.local
.env.*.local

# Agent tooling and the worktrees it creates under .claude/worktrees/
.claude/
